server:
  port: 9296


spring:
  application:
    name: CONFIG-SERVER.
  cloud:
    config:
      server:
        git:
          uri: https://github.com/anil-k-rajamoni18/config-server-api.git
          clone-on-start: true